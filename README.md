# Test Server

## 🚀 Quick Start

In the command line, copy the command below and hit **Enter**:

```bash
./freeCodeCamp/setup.sh && curl -L 'https://raw.githubusercontent.com/Web4Jobs/relational-database-test-server/master/server.js' -o freeCodeCamp/server.js && cd freeCodeCamp && npm install && npm install cors && npm install express && node server.js
```

## 📌 What this does

1. Runs the FreeCodeCamp setup script
2. Downloads the test server (`server.js`)
3. Places it inside the `freeCodeCamp` folder
4. Installs required Node.js dependencies
5. Starts the server

---

If you face any issues:

- Make sure **Node.js** and **npm** are installed
- Make sure the `freeCodeCamp` folder exists
- Check file permissions for `setup.sh`

Happy coding! 💻

